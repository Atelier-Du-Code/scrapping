services:
  - type: web
    name: scrapping
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn scrapping.wsgi:application"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: webapp.settings
